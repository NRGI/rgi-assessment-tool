div.left-padding-lg.right-padding-lg
    div.modal-header
        h3 Please edit comment
    div.modal-body
        .form-horizontal
            fieldset
                .row
                    .form-group(required).col-md-12
                        div(
                            name="comment_text"
                            ng-model="comment_content"
                            ta-toolbar="{{taToolbarOptions}}"
                            text-angular
                            ta-max-text="{{editorContentMaxLength}}")
    div.modal-footer
        button.btn.btn-success(ng-click="saveComment()" ng-disabled="!comment_content") Save
        | &nbsp;
        button.btn.btn-default(ng-click="closeDialog()") Cancel