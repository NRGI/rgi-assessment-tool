div.top-padding-med
    .well
        legend New interview reference
        .row.left-padding-lg(id="new_human_form")
            .form-horizontal
                fieldset
                    // EXISTING INTERVIEWEE
                    //TODO ALLOW USER TO SELECT FROM EXISTING REFERENCE SET
                    //div.row.form-group.col-lg-11
                    //    label Existing interviewee
                    //    select.form-group.form-control.pull-left(ng-model="existing_ref")
                    //        option(ng-repeat="ref in existing_ref" value="{{ref.value}}") {{ref.text}}
                    // NEW INTERVIEWEE
                    label New interviewee
                    div.row.form-group.col-lg-11
                        input.form-control.right-padding-med(
                        name="first_name"
                        type="text"
                        placeholder="First name (required)..."
                        ng-model="answer.human_ref_first_name"
                        autocomplete="off"
                        required)
                    div.row.form-group.col-lg-11
                        input.form-control.right-padding-med(
                        name="last_name"
                        type="text"
                        placeholder="Last name (required)..."
                        ng-model="answer.human_ref_last_name"
                        autocomplete="off"
                        required)
                    div.row.form-group.col-lg-11
                        input.form-control.right-padding-med(
                        name="email"
                        type="text"
                        placeholder="Email address (required)..."
                        ng-model="answer.human_ref_email"
                        autocomplete="off"
                        required)
                    div.row.form-group.col-lg-11
                        input.form-control.right-padding-med(
                        name="phone"
                        type="text"
                        placeholder="Phone number (required)..."
                        ng-model="answer.human_ref_phone"
                        autocomplete="off"
                        required)
                    div.row.form-group.col-lg-11
                        input.form-control.right-padding-med(
                        name="comment"
                        type="text"
                        placeholder="Comments..."
                        ng-model="answer.human_ref_comment"
                        autocomplete="off")
                    div.row.form-group.col-lg-11
                        datepicker(
                        date-format="{{date_format}}")
                            input.form-control(
                            ng-model="answer.human_ref_contact_date"
                            type="text"
                            placeholder="Date interviewed...")

                    //- BUTTON CONTROL
                    div.col-lg-5
                        button.btn.btn-block.btn-success(
                        ng-click="humanRefSubmit()"
                        ng-disabled="!answer.human_ref_first_name || !answer.human_ref_last_name || !answer.human_ref_email || !answer.human_ref_phone"
                        ) Create reference
                    div.col-lg-5
                        button.btn.btn-block.btn-danger(ng-click="closeDialog()") Cancel









