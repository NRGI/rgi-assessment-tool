language: node_js
node_js:
  - '0.10'

services:
  - docker

before_script:
  - npm install --dev
  - npm install -g bower
  - bower install
  - npm install phantomjs -g


script:
  - node_modules/karma/bin/karma start --browsers=PhantomJS --single-run